import Engine from "./Engine";
import { G } from "./main";

export default class BasicPostProcessor
{
  Update(dt: number)
  {
    // G.loadPixels();

    // for (let i = 0; i < G.pixels.length; i += 4)
    // {
    //   let r = G.pixels[i + 0];
    //   let g = G.pixels[i + 1];
    //   let b = G.pixels[i + 2];

    //   if (r == 10 && g == 10 && b == 10)
    //   {
    //     r = 255;
    //     g = 255;
    //     b = 0;
    //     G.pixels[i + 0] = r;
    //     G.pixels[i + 1] = g;
    //     G.pixels[i + 2] = b;
    //   }
    // }

    // G.updatePixels();
  }
}